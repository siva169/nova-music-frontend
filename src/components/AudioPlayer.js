export default function AudioPlayer() {
  // With the JioSaavn Native <audio> player implementation, the silent heartbeat 
  // is no longer required! Background play and lock screen media sessions are natively supported by Android Chrome 
  // for direct audio streams.
  return null;
}
